# Sessie · 2026-05-19

Eerste werkdag aan het SPIN-prototype. Inhoudelijk veel gedaan, install-fase
liep slecht. Onderaan: lessons voor volgende keer.

## Wat er staat op `claude/analyze-multi-agent-setup-dZJty`

### Multi-agent setup
- `CLAUDE.md` met SPIN-brief, tokens, motion-principes, multi-agent regels
- 5 custom subagents in `.claude/agents/` (explorer, planner, implementer, tester, reviewer)
- `.claude/settings.json` — subagents draaien op Sonnet, hooks, allowlist
- SessionStart-hook + `.claudeignore`

### Stack
- Next.js 15 (App Router) · React 19 · TypeScript strict
- Tailwind v4 met SPIN-kleuren als CSS-vars in `globals.css`
- Motion (motion/react) geïnstalleerd
- Radix primitives (dialog, popover, slider, tabs, toast, tooltip, slot)
- `next.config.ts` doet `output: 'export'`, `basePath: '/spin-app'`, `trailingSlash: true`, `images: { unoptimized: true }`
- **ESLint bewust eruit** (zie "Wat ging mis" hieronder)

### Mock data + types (subagent A)
- `src/types.ts` — domain model
- `src/lib/mock/*` — 30 reserveringen, 8 waitlist, tafelplan met posities, koelkast, activiteiten
- `src/lib/time.ts` — `MOCK_NOW = 2026-05-19T20:14:00+02:00` + `formatOccupancy()` voor "Speelt · 47m" taal

### Design primitives (subagent B)
- `src/components/ui/{Button,Field,Surface,Sheet,Badge,Tag}.tsx`
- `src/lib/cn.ts` (clsx + tailwind-merge)
- Barrel export

### Route shells (subagent C)
22 routes, alles statisch exporteerbaar:
- `/book/*` — mobile dark shell + step-dots: page, guests, extras, contact, deposit, confirmed, waitlist/[id] (countdown-ring placeholder)
- `/ops/*` — desktop light shell + linker nav: live dashboard, floor, reservations, waitlist, inbox, checkin, settings
- `/demo/*` — full-bleed dark shell + 4 hero placeholders: waitlist-claim, dashboard-live, floor-plan, bar-seating

### Deploy
- `scripts/deploy.sh` rsync naar `root@89.167.125.42:/var/www/dashboard/spin-app/`
- `pnpm run deploy` wired
- Live URL: `https://gerkedegroot.duckdns.org/spin-app/` (zodra `ssh-add ~/Developer/vps/hi` + `pnpm run deploy` gedraaid wordt vanaf de Mac)
- `docs/deploy.md` legt het volledig uit

## Wat ging mis

### Gokken op de install-stack
Ik heb in de sandbox `pnpm install` gedraaid zonder eerst Gerke's environment
te checken (Node-versie, pnpm-versie, andere projecten op zijn Mac). Resultaat:

1. Lockfile pinde `typescript-eslint@8.59.4` + `@unrs/resolver*@1.12.0` —
   beide gepubliceerd op 2026-05-18, dezelfde dag. Pnpm 11's default
   `minimum-release-age=24h` blokkeerde de install.
2. Sharp's postinstall werd ook geblokkeerd door pnpm 11's nieuwe
   `approve-builds` policy.
3. `pnpm.onlyBuiltDependencies` in `package.json` is door pnpm 11 deprecated
   (warning), maar de verhuizing naar `pnpm-workspace.yaml` hielp ook niet
   bij Gerke — onduidelijk waarom, niet uitgezocht.

### Wat we erover heen hebben gedaan (en in welke commits)
- `107a4e3` — `.npmrc` met `minimum-release-age=0` (deed niets bij Gerke)
- `b1696a1` — `pnpm.onlyBuiltDependencies` in package.json (deprecated)
- `c0b0039` — **ESLint volledig verwijderd** uit deps + lockfile regen
- `8c3b588` — `pnpm-workspace.yaml` met `onlyBuiltDependencies: [sharp]`
- Uiteindelijk werkend door: `pnpm approve-builds` interactief draaien

### UI ziet er nu zwaar onvoldoende uit
Screenshot van `/ops/waitlist` toonde aan: meer mis dan alleen fonts.

1. **Statische tijden** in placeholders ("18:34", "19:02") — CLAUDE.md
   verbiedt dit expliciet. Moet zijn "Gewacht · 14m" / "Claim sluit · 4:12".
   Subagent C heeft dit niet gevolgd; ik heb de output niet geverifieerd
   tegen de briefing voor ik het pushte.
2. **Fonts compleet fout** — system serif (Times-achtig) + system sans.
   Geen Parabolica, geen Fragment Mono. Halve identiteit weg.
3. **Generieke admin-tabel** — voelt als OpenTable / SaaS-dashboard, exact
   wat CLAUDE.md verbiedt ("boutique hotel × Japanse listening bar").
4. **Geen ops-intelligentie zichtbaar** — geen prioriteit-states, geen
   claim-windows, geen visuele reden waarom positie 1 anders is dan 5.
5. **Dood scherm** — geen motion, geen layered surface, geen sfeer.
6. **Fonts beschikbaar als TTF** in `~/Developer/spin/`, maar ik vroeg om
   woff2 zonder eerst te checken wat Gerke had. `next/font/local` werkt
   prima met TTF.

Geen design-pas geweest, alleen ruwe scaffolding die niet eens de hard
gestelde regels uit CLAUDE.md (occupancy-taal) volgde. Dat is een gat in
mijn proces, niet alleen tijdsdruk.

## Volgende sessie — in deze volgorde

1. **Check eerst environment + assets**: `node --version`, `pnpm --version`,
   welke andere projecten op de Mac staan, EN welke fonts/logo's Gerke
   al heeft in `~/Developer/spin/` (TTF, niet woff2!). Pas dán dependencies
   pinnen en assets inwiren.
2. **Fonts**: Parabolica + Fragment Mono TTF's uit `~/Developer/spin/`
   committen onder `src/fonts/`. Inwiren met `next/font/local` — Parabolica
   op `--font-display`, Fragment Mono op `--font-mono`. Hele typografie
   hangt hieraan.
3. **Logo**: SPIN SVG-mark inwiren in top-bars (book + ops layouts).
4. **Occupancy-taal pass**: alle statische tijden in placeholders vervangen
   door `formatOccupancy()` uit `src/lib/time.ts`. Subagent C heeft dit
   gemist — bij review oppakken.
5. **Landing-pass**: `/` moet bij eerste opening al "boutique hotel × Japanse
   listening bar"-gevoel hebben — niet generieke startup-page.
6. **Pas dáárna** hero-momenten. Eerst `/demo/waitlist-claim`: notificatie
   → fullscreen → countdown → claim → dashboard reordert. Motion-foundation
   die elders terugkomt.

**Belangrijk**: ik moet subagent-output verifiëren tegen CLAUDE.md vóór
committen, niet erná. Statische tijden hadden niet in een commit mogen
landen.

## Lessons voor mezelf

- **Niet bouwen op een lege omgeving zonder eerst de doel-omgeving te
  checken.** Wat in een fresh cloud-sandbox werkt, faalt op een Mac met
  pnpm 11 + Node 25 + supply-chain policies.
- **Niet gokken op flag-fixes** als er een verklarende error is en internet
  beschikbaar is. Eerst de pnpm-docs lezen, dan committen.
- **Bij subagents in parallel**: Gerke vond dit deel snel en goed werken.
  Voortzetten — maar de **scope per subagent moet kleiner** zodat ze niet
  per ongeluk overlappen of dingen schrijven die Gerke niet verwacht.
- **Geen ad-hoc dependencies meer toevoegen** in deze repo zonder eerst te
  controleren hoe oud die release is en of die op zijn Mac binnen het
  policy-window valt.

## Nachtsessie · ondergrens-Figma's binnen + design-pas

Na het tonen van twee bestaande Figma-screens ("dit is jullie ondergrens")
+ "doe rustig aan, ik ben er over een paar uur weer" — sweep gemaakt langs
alle key screens met echte SPIN-identiteit.

### Onderzoek vooraf (geen first-result-grab)
Vier sporen parallel via WebSearch + WebFetch:
- Awwwards · hotel/restaurant SOTD voor sfeer-references (The Drake Hotel,
  Move Hotels)
- Animation libs 2026: GSAP (nu free), Lenis (smooth scroll de-facto),
  Motion (al in use), Anime.js, Trig.js — gekozen Motion+Lenis stack
- Component libs: HeroUI, Aceternity (paid voor de mooie), Magic UI
  (open source, copy-paste), 21st.dev — uiteindelijk **eigen primitives**
  gebouwd ipv kit-defaults, scheelt vechten met SPIN-tuning
- Lenis · Next App Router 2026: nieuwe `lenis` package (oude
  `@studio-freight/*` retired), `prefers-reduced-motion` check

### Toegevoegde foundations
- `next/font/google` · Fraunces + Fragment Mono als stand-ins (Fragment
  Mono is écht op Google Fonts; Fraunces is de beste Parabolica-proxy).
  Swap naar `next/font/local` = één-line change zodra de TTF's in
  `src/fonts/` staan.
- `app/globals.css` · warm vignette, layered card shadow, grain texture,
  tabular numerals helper, live-pulse keyframe, `spin-em` italic class.
- `components/brand/SpinMark.tsx` · text-mark in display-bold (tot SVG-mark
  binnen is)
- `components/motion/*` · zelf gebouwd, niet uit kit:
  `SmoothScroll` (Lenis wrapper), `NumberTicker` (spring count-up),
  `BlurFade` (in-view reveal), `AnimatedList` (layout reorder),
  `CountdownRing` (live tick + warn-color), `Marquee`
- `lib/queries.ts` · resolves mock data naar render-shapes
  (getDashboardMetrics, getUpcomingArrivals, getResolvedWaitlist,
  getAttentionItems, formatClock, secondsUntil)
- `lenis` toegevoegd als dependency

### Screens echt herbouwd (van scaffolding → design)
| Route | Wat |
|---|---|
| `/` | Cinematische landing met staggered reveal, italic accent op "jouw tafel.", orange/outline CTA pair, HouseRulesSheet bottom-sheet voor "Hoe het werkt" |
| `/ops` layout | Dark sidebar met diamond markers, live badges via queries, orange CHECK-IN CTA met QR-glyph, footer met klok |
| `/ops` dashboard | 4 metric cards met NumberTicker, dark-header arrivals table met live status badges, purple priority section met "Trigger vrijgave" |
| `/ops/waitlist` | Claim-window hero met live CountdownRing, AnimatePresence list met spring reorder, simuleer-vrijgave knop |
| `/ops/reservations` | Filterable table (vanavond/actief/aandacht/alles), search, status badges, occupancy-taal kolom |
| `/ops/inbox` | Attention-feed met type-coding (late, vip, birthday, system, suspicious), relative tijd, ongelezen-indicator |
| `/ops/floor` | Echte SVG-plattegrond uit TABLES-data, zone-labels, counter-strip, click voor gastdetails |
| `/ops/checkin` | QR-scanner met scanning animation, sub-grid van verwachte aankomsten met inline check-in CTAs |
| `/ops/settings` | Tab-nav met layoutId pill, animated-content-swap per sectie |
| `/book` | Date picker matching Figma reference 3 — day grid, tijdslot-states (vrij/beperkt/vol/selected) met sticky footer summary |
| `/book/guests` | Party-size selector + 3 activity cards met expandable details |
| `/book/extras` | KOELKAST_PACKAGES met expandable item lists, signature-badge op SPIN Signature |
| `/book/contact` | Form fields, preferences-chips (toggle), opmerking-textarea |
| `/book/deposit` | Summary card + payment method radio (iDEAL/CC/Apple Pay) met layoutId-indicator |
| `/book/confirmed` | Animated check-mark, reference card, agenda/share affordances |
| `/book/waitlist/[id]` | Live CountdownRing, table details, claim CTA |
| `/demo` index | 4 hero-cards met accent-strip-hover, duur per scene |
| `/demo/waitlist-claim` | Cinematische 4-stage sequence: intro → notification (slide-in van boven) → claim (countdown + details) → confirmed (animated check + dashboard preview). Stage-indicator dots onderaan |
| `/demo/dashboard-live` | Real-time tick · elke 2s update: rows advanceren (soon → arriving → active → overtime), sortering reorganiseert op urgentie, NumberTicker op metrics, periodieke nieuwe arrival |

### Wat nog (volgende sessie)
1. **Echte fonts**: TTF's uit `~/Developer/spin/` committen onder
   `app/fonts/` of `public/fonts/`. In `app/fonts.ts` `next/font/local`
   wiring naast (of vervangend van) Fraunces/Fragment Mono. Variabele
   namen blijven `--font-parabolica` en `--font-fragment-mono`.
2. **Logo SVG**: SPIN-logo SVG uit assets erin, `SpinMark` component
   omzetten naar inline SVG ipv text.
3. **Sfeerfoto's**: als beschikbaar, gebruiken voor landing of een
   listening-bar surface achter `/demo/bar-seating`.
4. **`/demo/floor-plan`** en **`/demo/bar-seating`** kunnen nog een
   pass krijgen — werken nu maar voelen iets minder gepolijst dan de
   andere demos.
